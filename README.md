# Kiosk-Example (임시 Readme)
KNU graduation project - Kiosk Example

# 패키지 설치
pip install pygame

pip install gtts

pip install SpeechRecognition 

pip install deep_translator

pip install nltk

pip install word2number

pip install num2words
